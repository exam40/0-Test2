{
  "name": "exam-countdown-planner",
  "version": "0.9.0",
  "description": "Browser-only study planner for Singapore national exams (GCE O/NA/NT/A-Level).",
  "private": true,
  "scripts": {
    "lint": "eslint public/script.js",
    "check": "node --check public/script.js",
    "test": "echo \"No test framework configured yet\" && exit 0",
    "serve": "python -m http.server 8080 --directory public"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
