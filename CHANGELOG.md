# Changelog

## v0.9 — Results, Trust & Offline
- Results & Grade Target tab: per-paper weights, editable targets, single current score, projected grade band (approx ±3%).
- Interactive grade-region graph with draggable slider (pointer events, pointer capture).
- Example score combinations, impossible-target warnings, weighted-average math explanation (`<details>`), per-mark "of final" contribution.
- Grade-boundary estimates disclaimer + "(est.)" badges throughout.
- Named autosave profiles (snapshot save/load/delete).
- Achievements / milestones.
- Offline PWA: `manifest.json`, `service worker`, install banner.
- Lazy-loaded OCR / PDF libraries (no longer block first load).
- Yearly exam-date update via external JSON file.
- Global error logging with About / Error Log view.
- What's New (changelog) modal.
- Printable timetable, `.ics` export, JSON backup/restore, undo/redo, study streaks, FAQ, dark mode.

## v0.8
- Major UX and Results improvements (see earlier work): graph clipping, combos, streak, print view, revised undo/redo.

## v0.1
- Initial exam countdown planner with setup wizard and built-in SEAB sets.